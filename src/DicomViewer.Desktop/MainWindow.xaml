<Window x:Class="DicomViewer.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DicomViewer.Desktop.ViewModels"
        Title="DICOM ROI Analyzer" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Icon="app.ico">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Window.InputBindings>
        <KeyBinding Key="Z" Modifiers="Ctrl"
                    Command="{Binding UndoRoiCommand}"/>
    </Window.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <Grid Grid.Row="0" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Margin="0,0,5,0"
                     Text="{Binding FolderPath, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center"
                     KeyDown="FolderPathTextBox_KeyDown"/>
            <Button Grid.Column="1" Content="Browse" Padding="10,2" Margin="0,0,5,0"
                    Click="BrowseButton_Click"/>
            <Button Grid.Column="2" Content="Reload" Padding="10,2" Margin="0,0,5,0"
                    Command="{Binding ReloadCommand}"/>
            <Button Grid.Column="3" Content="Settings" Padding="10,2"
                    Click="SettingsButton_Click"/>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Tree + Filter -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TreeView Grid.Row="0" Margin="5"
                          ItemsSource="{Binding TreeGroups}"
                          SelectedItemChanged="TreeView_SelectedItemChanged">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate
                            DataType="{x:Type vm:TreeGroupViewModel}"
                            ItemsSource="{Binding Children}">
                            <TextBlock Text="{Binding Label}"/>
                            <HierarchicalDataTemplate.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:TreeFileViewModel}">
                                    <TextBlock Text="{Binding DisplayName}"/>
                                </DataTemplate>
                            </HierarchicalDataTemplate.ItemTemplate>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
                <TextBox Grid.Row="1" Margin="5"
                         Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"/>

            <!-- Right Panel -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Controls Row -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
                    <Button Content="{Binding IsPlaying, Converter={StaticResource PlayPauseConverter}}"
                            Padding="10,2" Margin="0,0,5,0"
                            Command="{Binding TogglePlaybackCommand}"
                            IsEnabled="{Binding HasFiles}"/>
                    <Button Content="Previous" Padding="10,2" Margin="0,0,5,0"
                            Command="{Binding PreviousFileCommand}"
                            IsEnabled="{Binding HasFiles}"/>
                    <Button Content="Next" Padding="10,2" Margin="0,0,5,0"
                            Command="{Binding NextFileCommand}"
                            IsEnabled="{Binding HasFiles}"/>
                    <Button Content="Clear ROI" Padding="10,2" Margin="0,0,5,0"
                            Command="{Binding ClearRoiCommand}"
                            IsEnabled="{Binding HasFiles}"/>
                    <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"
                               Margin="5,2"/>
                    <TextBlock Text="ROI:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox x:Name="RoiShapeCombo" Width="100"
                              SelectedIndex="0" SelectionChanged="RoiShapeCombo_SelectionChanged"
                              IsEnabled="{Binding HasFiles}">
                        <ComboBoxItem Content="Rectangle"/>
                        <ComboBoxItem Content="Ellipse"/>
                        <ComboBoxItem Content="Freeform"/>
                    </ComboBox>
                    <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"
                               Margin="5,2"/>
                    <TextBlock Text="FPS:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <Slider Width="100" Minimum="1" Maximum="30"
                            Value="{Binding Fps}"
                            VerticalAlignment="Center"
                            IsSnapToTickEnabled="True" TickFrequency="1"
                            IsEnabled="{Binding HasFiles}"/>
                    <TextBlock Text="{Binding Fps}" VerticalAlignment="Center" Margin="5,0,0,0"/>
                </StackPanel>

                <!-- File Info -->
                <TextBlock Grid.Row="1" Margin="5,2" FontWeight="SemiBold"
                           Text="{Binding FileInfoText}"/>

                <!-- Image + Tags Split -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Image Viewer -->
                    <Border Grid.Column="0" Margin="5" BorderBrush="Gray"
                            BorderThickness="1" Background="Black">
                        <Canvas x:Name="ImageCanvas" ClipToBounds="True"
                                MouseLeftButtonDown="ImageCanvas_MouseLeftButtonDown"
                                MouseMove="ImageCanvas_MouseMove"
                                MouseLeftButtonUp="ImageCanvas_MouseLeftButtonUp"
                                SizeChanged="ImageCanvas_SizeChanged">
                            <Image x:Name="DicomImageControl"
                                   Source="{Binding CurrentImage}"
                                   Stretch="Uniform"/>
                        </Canvas>
                    </Border>

                    <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"/>

                    <!-- Tag Panel -->
                    <Border Grid.Column="2" Margin="5" BorderBrush="Gray" BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="DICOM Tags" FontWeight="Bold" Margin="5"/>
                            <ListBox x:Name="TagListBox" Grid.Row="1"
                                     ItemsSource="{Binding DicomTags}"
                                     FontFamily="Consolas" FontSize="11"
                                     SelectionMode="Extended">
                                <ListBox.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="Copy Selected Tag(s)"
                                                  Click="CopySelectedTags_Click"
                                                  InputGestureText="Ctrl+C"/>
                                        <MenuItem Header="Copy All Tags"
                                                  Click="CopyAllTags_Click"/>
                                    </ContextMenu>
                                </ListBox.ContextMenu>
                            </ListBox>
                        </Grid>
                    </Border>
                </Grid>

                <!-- ROI Mean + Status -->
                <Grid Grid.Row="3" Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{Binding RoiMeanText}"
                               FontWeight="SemiBold" Margin="0,0,20,0"/>
                    <TextBlock Grid.Column="1" Text="{Binding StatusText}"
                               Foreground="Gray"/>
                </Grid>
            </Grid>
        </Grid>

        <!-- Frame Slider -->
        <Grid Grid.Row="2" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Frame:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <Slider Grid.Column="1"
                    Minimum="0" Maximum="{Binding MaxFrame}"
                    Value="{Binding CurrentFrame}"
                    VerticalAlignment="Center"
                    IsSnapToTickEnabled="True" TickFrequency="1"
                    ValueChanged="FrameSlider_ValueChanged"
                    IsEnabled="{Binding HasFiles}"/>
            <TextBlock Grid.Column="2" Text="{Binding FrameText}"
                       VerticalAlignment="Center" Margin="5,0,0,0"/>
        </Grid>
    </Grid>
</Window>
